# HerBibleApp 真机 / 模拟器 冒烟测试脚本

> 代码审计(见 TEST_REPORT.md)覆盖不了的运行时验证清单。**每一步标了「预期」**,不符就是 bug,记下来。
> 装包:模拟器用 `eas build --profile preview`(产出 APK,可直接拖进模拟器);真机最终验收用正式 AAB 转的包。
> ⚠️ R8 是首次开启,**必须用 release/正式包**跑一遍,debug 包测不出 R8 裁剪问题。

---

## 0. 准备
- [ ] 装新包到一台**真机** + 一台**低端机/模拟器**(测性能/内存)。
- [ ] Firebase 控制台打开 **DebugView**;真机执行 `adb shell setprop debug.firebase.analytics.app com.holy.bible.kjv.audio.prayer` 让事件实时进 DebugView。
- [ ] 准备一个能切弱网/飞行模式的环境。

## 1. 全新用户首启(冷启动 #1)
- [ ] 杀进程清数据后首次打开 → **预期**:只看到白粉渐变欢迎页(icon + Her Bible + tagline),约 2.5 秒自动进主界面,**不弹任何通知权限**。
- [ ] 主界面正常渲染,无白屏、无闪烁回跳。
- [ ] DebugView 是否收到启动/首屏事件 → **预期**:有(若无,印证 P0-2 埋点缺失)。

## 2. 第二次冷启动 + 通知引导
- [ ] 杀进程,**第二次**冷启动(同一天或第二天)→ **预期**:出现 FollowHim 通知引导页。
- [ ] 点 **Not allow / 拒绝** 系统弹窗 → **预期**:直接进 app,**不跳系统设置**。
- [ ] 再次触发引导,点 **Continue** 允许 → **预期**:授权后,以后不再弹;NotificationsScreen 里开关已开。

## 3. 祷告核心流程
- [ ] 早祷:点 Start Morning Prayer → 走完 → 点 **Amen** → **预期**:庆祝页出现双手合十 Lottie + 彩带播放一次,Continue 可点。
- [ ] (batch 2 起)经文页 reference 旁有 **ⓘ**;首次进入弹一次性聚光引导 → 点 ⓘ → **预期**:白卡弹窗显示该节背景小注,Got it/背景点击/安卓返回键都只关弹窗不退流程;引导终生只出现一次。
- [ ] (batch 2 起)祷告流内**无 Listen 按钮**(每日经文旁白暂停,音频后补;圣经阅读器的朗读不受影响)。
- [ ] (法/德圣经)诗篇 20/4/56/46/42 五条位移节:主屏卡、流程页、收藏、Read full chapter 跳转高亮、历史页 → **预期**:五处编号一致(本地编号),跳章高亮的正是刚读的那节。
- [ ] 返回主界面 → **预期**:早祷卡显示已完成态。
- [ ] 晚祷同样走一遍。**早晚都完成后** → 祷告天数 +1。
- [ ] **跨午夜测试**:把系统时间调到次日凌晨,App 保持后台再切回 → **预期**:今日记录滚动到新一天(若仍显示昨天完成态,印证 P1-1)。
- [ ] Amen 时如有广告 → **预期**:不崩溃(弱网下尤其);广告时机是否打断庆祝体验,记录主观感受。

## 4. Streak / 祷告天数页
- [ ] 进祷告天数页 → **预期**:大火苗是动画 Lottie;今日未做=**强调色圆环在缓慢旋转**(无火苗);只做一半=**很淡的火苗**;早晚都做=满火苗。
- [ ] 晚上进这个页面看今日环颜色 → **预期**:应随时段(若永远玫红,印证 P1-6)。
- [ ] 主界面右上角徽章数字、Profile 的 Days Prayed、本页大字 → **预期:三处一致**。
- [ ] "Why Streaks?" 弹窗的 ✕ → **预期**:容易点到、能关掉。
- [ ] 开始日期 → **预期**:= 你第一次祷告那天(哪怕只做了一次)。

## 5. Gospel & Psalm 新功能
- [ ] 主界面 Start 按钮下方 → **预期**:两张白卡,一张 Morning 一张 Evening,标题 `Gospel & Psalm (1/89)`。
- [ ] 点 Morning → **预期**:上段福音章 + 下段诗篇,配晨图;右上角音乐键可开关背景乐(不是真人朗读);share/copy 可用;Amen 标记完成 → 卡片打绿勾。
- [ ] 点 Evening → **预期**:只有诗篇,配晚图。
- [ ] 早上把早晚都做完 → **预期**:次日才解锁 Day 2(当天不解锁)。
- [ ] 反复进出阅读页 + 切背景乐 → **预期**:音乐不卡顿/不重叠/不残留(留意 P1-4)。
- [ ] 切到诗篇 119 那几天 → **预期**:长章滚动不卡死(留意 P1-12)。

## 6. 圣经阅读 + 解释(en-mh)
- [ ] 打开任意已翻译章节的 Explore/解释 → **预期**:显示新的 Claude 解释(不是 Tyndale)。
- [ ] 圣经朗读:点播放 → **预期**:真人朗读(非 TTS);逐句高亮跟随;切到别的 Tab → **预期**:继续播 + 右缘出现迷你药丸(X 可停)。
- [ ] 朗读中退到桌面/锁屏 → **预期**:继续播;通知栏/锁屏出现媒体控件(播放/暂停/快进退),标题=书名+章号;锁屏暂停再播放均生效。
- [ ] 朗读中打开任意其他 App 的音乐/视频 → **预期**:我方自动暂停(Android 通知栏控件保留可手动恢复;iOS 锁屏 Now Playing 归属对方属系统行为,回 app 恢复即可)。
- [ ] 朗读中进入祷告流程 → **预期**:圣经朗读停止、媒体控件消失,祷告音频正常(双播放器混音不受影响)。
- [ ] 朗读时调静音键 → 记录是否仍出声(P1-13 音频会话)。

## 7. 登录
- [ ] Sign in → 点 **Sign in with Google** → **预期**:按钮有缩放反馈 + loading 动画;账号选择弹窗尽快出现;选号后登录成功。
- [ ] **Facebook 登录** → **预期(现状)**:大概率失败(FB App ID 还是占位符,P0-5);记录实际表现。
- [ ] 登录后 DebugView → **预期**:有 login 事件且带 method(若无,印证 P0-2)。
- [ ] 退出登录 → 重新冷启动 → **预期**:登录态正确(不应"自动又登录"或状态不一致)。

## 8. 广告 / 变现
- [ ] 正常网络触发插屏(完成祷告/读经日)→ **预期**:能弹出(现在是测试广告);关闭后不崩。
- [ ] **弱网/飞行模式**下触发 → **预期**:不崩溃、不卡 UI、流程照常继续(广告加载失败应静默跳过)。
- [ ] 连续快速触发两次 → **预期**:不连弹两个广告(留意 P1 并发去重)。
- [ ] 进"移除广告"页点订阅 → **预期(现状)**:只弹 coming soon(IAP 未接,P0-4);记录。

## 9. 弱网 / 离线 韧性(重点!对应 P0-1)
- [ ] **弱网**下进圣经章节 / Gospel&Psalm → **预期**:加载慢但最终成功 **或** 明确报错;**绝不能无限转圈**(若无限转,印证 P0-1 无超时)。
- [ ] **飞行模式**下进各页 → **预期**:有缓存的显缓存,无缓存的优雅报错,不白屏不崩溃。
- [ ] 飞行模式进祷告页 → **预期**:背景图回退到内置图(不是 app 图标铺满)。

## 10. 中断 / 并发 / 稳定性
- [ ] 祷告/朗读播放中切到别的 App(微信/电话)再切回 → **预期**:不崩溃,音频行为合理。
- [ ] 来电话 / 闹钟打断音频 → 切回 → **预期**:不崩溃。
- [ ] 后台挂很久(几小时)再切回 → **预期**:正常,日期/内容刷新到当天。
- [ ] 快速连点各按钮、快速返回 → **预期**:不崩溃、不卡死(留意裸 setTimeout 卸载后 setState)。
- [ ] 低存储 / 低内存设备跑成就页、诗篇119 → **预期**:不 OOM/ANR。

## 11. 多语言
- [ ] 切到简中/繁中/德/法/西/葡各扫一遍主流程 → **预期**:无 `key.path` 原样裸露;Gospel & Psalm 新文案已翻译;占位符 `(1/89)` 正确。

## 12. Firebase 数据验证(在 Chrome 看 DebugView)
- [ ] 跑完上面流程后,DebugView 里应能看到:启动、屏幕浏览、祷告完成、计划完成、登录、分享、广告展示等事件。
- [ ] **现状预警**:若大量事件缺失,即 P0-2(埋点零调用)。这是上线前必须补的。
- [ ] 制造一次崩溃(或看真实崩溃)→ Crashlytics 是否收到 → **预期**:有(否则印证 recordError 未接 + R8 可能裁了 Crashlytics)。

---

### 发布门禁(必须全绿才全量)
1. 弱网无限 loading = 0(P0-1)
2. 关键事件在 DebugView 可见(P0-2)
3. 正式 AdMob ID 已替换、广告能正常展示(P0-3)
4. 付费墙能成交或已下线(P0-4)
5. 登录(至少 Google)在**正式 release 包**真机成功(P0-5/6)
6. release 包全链路无 R8 导致的崩溃(P0-6)
7. 字体/资源异常不白屏(P0-7)
