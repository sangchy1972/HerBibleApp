// Minimal stub. Original was the Android widget task handler (lost when a
// worktree was deleted; never committed). Only the storage key constant
// is referenced by AddWidgetScreen.tsx; the actual handler logic must be
// restored before widget functionality works.
export const WIDGET_VERSE_KEY = 'verse-of-day-widget';
